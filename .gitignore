node_modules/
.expo/
.expo-shared/
dist/
web-build/
# Expo prebuild outputs — only the top-level ios/ + android/ folders (where
# prebuild generates native projects), NOT nested `store/ios/` or
# `store/android/` directories used for store metadata.
/ios/
/android/
coverage/

# Env
.env
.env.local

# Per-developer contact info for the Impressum — the user creates this
# locally and never commits their real address/phone/email.
src/screens/Legal/contact.local.ts

# EAS
.easignore

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Drizzle
drizzle/meta/_journal.json.bak

# Project-reviewer skill output — timestamped review reports live here, not in git
.reviews/

# Claude Code local settings
.claude/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli